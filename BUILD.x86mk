@sysdef OUT.DIR img
@loaddefs DOS.cfg
@default: all
debug:
  @sysdef DEBUG -v
all: 
  @target kernel
  @return
  
kernel:
  @changed: KERNEL.c
  @ramfile KERNEL
  PP KERNEL.c -R RAM_KERNEL $KERNPP
  CC -r RAM_KERNEL -R RAM_KERNEL $KERNCC
  ASM -r RAM_KERNEL -o KERNEL $KERNASM
  @return
